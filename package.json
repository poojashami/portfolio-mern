{
  "name": "portfolio-mern-root",
  "version": "1.0.0",
  "scripts": {
    "start-server": "cd server && npm run dev",
    "start-client": "cd client && npm run dev",
    "install-all": "npm install --prefix client && npm install --prefix server"
  }
}
